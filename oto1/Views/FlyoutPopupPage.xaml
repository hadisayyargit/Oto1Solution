<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:oto1"
             
             x:Class="oto1.FlyoutPopupPage"
             Title="myFlyoutPopupPage">
    <CollectionView x:Name="collectionViewMenu" ItemsSource="{Binding objFlyoutPageItems}"                   x:FieldModifier="public"                  SelectionMode="Single">

        <CollectionView.ItemTemplate>
            <!--<DataTemplate>
                    <Label    Text="{Binding Title}"    Grid.Row="0"  Grid.Column="1"  FontSize="20"        FontAttributes="Bold"        VerticalOptions="Center" Margin="10">

                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnLabelTapped" />
                        </Label.GestureRecognizers>
                    </Label>
            </DataTemplate>-->

            <DataTemplate>
                <Grid Padding="5,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image Source="{Binding IconSource}" />
                    <Label Grid.Column="1"
                           Margin="20,0"
                           Text="{Binding Title}"
                           FontSize="20"
                           FontAttributes="Bold"
                           VerticalOptions="Center" />
                </Grid>
            </DataTemplate>

        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>