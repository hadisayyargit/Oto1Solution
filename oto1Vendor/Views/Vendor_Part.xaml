<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="oto1.Vendor_Part"
             Title="Vendor_Part"
    xmlns:dg="clr-namespace:Maui.DataGrid;assembly=Maui.DataGrid"
             
             >
    <Grid RowDefinitions="70,*">

        <Button  Text="Clear" Clicked="btnClear_Clicked" Grid.Row="0"></Button>
        <dg:DataGrid x:Name="grdVendorPart" ItemsSource="{Binding }" SelectionMode="Single"  SelectedItem="{Binding SelectedPartVendor}"
                RowHeight="70" HeaderHeight="50" BorderColor="{StaticResource GridBorderColor}"
                HeaderBackground="{StaticResource GridHeaderBgColor}" HeaderBordersVisible="{Binding HeaderBordersVisible}"
                 PullToRefreshCommand="{Binding RefreshCommand}" IsRefreshing="{Binding IsRefreshing}" PaginationEnabled="True" PageSize="5"
                ActiveRowColor="{StaticResource ActiveRowColor}" Grid.Row="1">
            <dg:DataGrid.NoDataView>
                <Label Text="Empty"></Label>
            </dg:DataGrid.NoDataView>
            <dg:DataGrid.Columns>
                <dg:DataGridColumn Title="" PropertyName="ThumbnailPhotoFile" SortingEnabled="False">
                    <dg:DataGridColumn.CellTemplate>
                        <DataTemplate >
                            <Image Source="{Binding}" HorizontalOptions="Center" VerticalOptions="Center"
                           Aspect="AspectFit" HeightRequest="60" />*-
                        </DataTemplate>
                    </dg:DataGridColumn.CellTemplate>
                </dg:DataGridColumn>
                <dg:DataGridColumn Title="Id" PropertyName="Id" Width="0.5*" IsVisible="True" />
                <dg:DataGridColumn Title="قطعه" PropertyName="FName" IsVisible="{Binding Fname}" Width="{Binding FNameColumnWidth}" />

                <dg:DataGridColumn Title="از تاریخ" PropertyName="jalaliBeginDate" Width="70" />
                <dg:DataGridColumn Title="تا تاریخ" PropertyName="JalaliEndDate" Width="70" />

                <!--<dg:DataGridColumn PropertyName="Home">
                    <dg:DataGridColumn.FormattedTitle>
                        <FormattedString>
                            <Span Text="Home" TextColor="Black" FontSize="13" FontAttributes="Bold" />
                            <Span Text=" (won-lost)" TextColor="#333333" FontSize="11" />
                        </FormattedString>
                    </dg:DataGridColumn.FormattedTitle>
                </dg:DataGridColumn>-->
                <dg:DataGridColumn Title="قیمت" PropertyName="PriceAmount" Width="70" StringFormat="{}{0:0.00}" />
                <dg:DataGridColumn Title="تخفیف٪" PropertyName="DiscountPercent" Width="70" StringFormat="{}{0:0.00}" />
                <!--<dg:DataGridColumn Title="Streak" PropertyName="Streak" Width="0.75*">
                <dg:DataGridColumn.CellTemplate>
                    <DataTemplate x:DataType="m:Streak">
                        <ContentView HorizontalOptions="Fill" VerticalOptions="Fill"
                                 BackgroundColor="{Binding Converter={StaticResource StreakToColorConverter}}">
                            <Label Text="{Binding}" HorizontalOptions="Center" VerticalOptions="Center"
                               TextColor="Black" />
                        </ContentView>
                    </DataTemplate>
                </dg:DataGridColumn.CellTemplate>
            </dg:DataGridColumn>-->

                <dg:DataGridColumn PropertyName="." Width="0.75*" FilteringEnabled="False">
                    <dg:DataGridColumn.CellTemplate>
                        <DataTemplate >
                            <Button Text="Edit" BackgroundColor="LightSkyBlue" Command="{Binding Commands[EditCommand], Source={RelativeSource Mode=FindAncestorBindingContext}}" CommandParameter="{Binding .}" />
                        </DataTemplate>
                    </dg:DataGridColumn.CellTemplate>
                    <!--<dg:DataGridColumn.EditCellTemplate>
                        <DataTemplate >
                            <Button Text="Done" BackgroundColor="MediumSeaGreen" Command="{Binding Commands[CompleteEdit], Source={RelativeSource Mode=FindAncestorBindingContext}}" CommandParameter="{Binding .}" />
                        </DataTemplate>
                    </dg:DataGridColumn.EditCellTemplate>-->
                </dg:DataGridColumn>

            </dg:DataGrid.Columns>
            <dg:DataGrid.RowsBackgroundColorPalette>
                <dg:PaletteCollection>
                    <Color>#F2F2F2</Color>
                    <Color>#FFFFFF</Color>
                </dg:PaletteCollection>
            </dg:DataGrid.RowsBackgroundColorPalette>
        </dg:DataGrid>
    </Grid>
</ContentPage>
